# Environment Variables

# DEVELOPMENT MODE (INSECURE - FOR TESTING ONLY)
# Exposes OpenAI API key on client-side
# DO NOT USE IN PRODUCTION
VITE_OPENAI_API_KEY=your_openai_api_key_here

# PRODUCTION MODE (SECURE)
# Calls Supabase Edge Function that proxies to OpenAI
# API key stays on backend, never exposed to client
VITE_EDGE_FUNCTION_URL=https://your-project.supabase.co/functions/v1/chat-ai

# MODE SELECTION
# Set to 'development' for direct OpenAI calls (insecure, testing only)
# Set to 'production' for Edge Function calls (secure, production-ready)
VITE_MODE=development
